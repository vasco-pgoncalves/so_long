#include "so_long.h"

int	shape_check(t_data *data)
{
    int len;
    int i;
    i = 0;
    len = ft_strlen (data->map[i])
    while (data->*map[i]== len)
    {
        if(chegar ao fim do mapa)
        {
            return (1);
        }
        i++;
    }
    else
        return (0)
}

int wall_chacker(t_data *data)